# Production — use your deployed API URL (HTTPS required)
# EXPO_PUBLIC_API_URL=https://your-project.vercel.app

# Local dev — web/emulator
# EXPO_PUBLIC_API_URL=http://localhost:3000

# Local dev — phone on same Wi-Fi (replace with your PC's LAN IP)
# EXPO_PUBLIC_API_URL=http://YOUR_LAN_IP:3000
